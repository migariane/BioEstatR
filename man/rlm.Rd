% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rlm.R
\name{rlm}
\alias{rlm}
\title{Regresion lineal multiple}
\usage{
rlm(
  f,
  data = NULL,
  pred = NULL,
  grf = TRUE,
  dfout = FALSE,
  alfa = 0.05,
  conf = 1 - alfa,
  decs = 3
)
}
\arguments{
\item{f}{formula: especificacion del modelo (ej. y ~ x1 + x2)}

\item{data}{data.frame: data table}

\item{pred}{data.frame: regressors for model prediction}

\item{grf}{logical: if grf=FALSE, graphical output is omitted}

\item{dfout}{logical: if dfout=TRUE, the procedure returns the data matrix with residuals and predictions}

\item{alfa}{real < 1: alpha error (parameter alternatively to confidence level). Default = 0.05.}

\item{conf}{real < 1: confidence level for effect estimation IC. Default = 1-alfa.}

\item{decs}{integer: decimal precision for results. Default = 3.}
}
\value{
Report with descriptive measures, parameter estimation for multiple linear regression, residual description, and diagnostic plots
}
\description{
Ajusta el modelo de regresion lineal multiple de acuerdo a la especificacion y ~ x1 + x2 + ...
}
\examples{
# Example 1 - Basic usage
data(osteo)
rlm(imc ~ peso + talla, data = osteo)

# Example 2 - With predictions
data(osteo)
new_data <- data.frame(peso = c(70, 80), talla = c(170, 180))
rlm(imc ~ peso + talla, data = osteo, pred = new_data)

# Example 3 - English output
data(osteo)
options(BioStatR.lang = "en")
rlm(imc ~ peso + talla, data = osteo)
options(BioStatR.lang = "es")
}
\references{
Montgomery, D. C., Peck, E. A., & Vining, G. G. (2012). Introduction to Linear Regression Analysis.
}
